# GODSEYE — PRODUCT SPEC & BRAND SYSTEM (authoritative)
> DV's direction, Aug 11 2026. "Don't argue with me — everything I've talked about should be set up like this. I'm creating my own value."
> Source of truth for how the product, pages, pricing, community, and marketplace are built.

---

## 0. NOMENCLATURE (hard rule — never break)
- **Hermes** is the invisible engine in the background. Transcription of DV's speech may render it "Hemis" / "Hemi's" — ALWAYS resolve to **Hermes**.
- **NEVER mention "Hermes-as-a-service"** to customers/public. We REWRAP Hermes under OUR brand. It is the recursive open-source instance we run, in the background.
- Brand story: **"We are the hardware company for agents."** Vanilla agents drop in → you customise them → or pull components from the marketplace.

---

## 1. THREE-PAGE STRUCTURE
| Page | Role | What lives there |
|------|------|------------------|
| **GODSEYE** (`godseye.digitalhustlerx.com`) | MAIN OFFICIAL PRODUCT page | The product: signup, running agent, dashboard, pricing, marketplace |
| **LIVING WITH AGENTS** | OPEN-SOURCE COMMUNITY page | Tips, hacks, docs, how-I-use-it-and-grow, monthly paid membership, benefits |
| **DV PERSONAL BRAND page** | Founder | Personal brand, bio, build-in-public, the "no-screen" pioneer story |

They stand side by side: Godseye = official/product. Living with Agents = community. Personal = founder.

---

## 2. ONBOARDING / SIGNUP FLOW (the core promise)
When a user **creates an account**, they IMMEDIATELY get:
- A **RUNNING agent** they can converse with right away (not a waitlist, not a placeholder).
- Their **plugin** and their **LLM API keys**.
- We **resell our OpenCode + GLM keys for cheap** so they don't need their own.
- **Both referral links** included in the flow:
  - OpenCode: `https://opencode.ai/go?ref=PCC7NY4J9S`
  - GLM referral link
- The user connects their **own Telegram bot** to their agent so they operate where they already live (Telegram-first).

## 3. FILE / STORAGE MODEL
- We share by **storage** (not just inference).
- An **"agents" file** manages each user's primary/main file.
- Users can create **other stores** ("create other stores where we put it").

## 4. MARKETPLACE (plugins)
- **Plugins = directives to your LLM.** e.g. a plugin that sets up a **Telegram group environment** end-to-end.
- Shown under **"Recommended Plugins"**.
- **Affordable base price** (fast-track convenience vs doing it manually).
- **STORE = "coming soon" — NOT purchasable yet.** DV curates the store later; the "coming soon" state creates scarcity. Users get the vanilla base agentic system now; plugins come later.

## 5. BASE PRODUCT
- A **vanilla, recursive Hermes open-source instance** (the one we run) — the base agentic system every user starts with.

## 6. PRICING (REWRITE REQUIRED — do this now)
- **Rewrite the pricing section on the Godseye main domain AND the G4 variant.**
- Kill the **WordPress-only framing** — that's stale. We are NOT "a WordPress tool." We're the hardware company for agents (business run by an agent fleet on Telegram).
- Set the pricing plan from the model described here (founders' credit bonus + onboard-now + agent-hardware tiers), NOT WordPress credits.

---

## 7. LAUNCH STATE (what's live vs what DV curates later)
| Item | State |
|------|-------|
| Signup → instantly-running agent | Build (dev prototype proves the signup→isolated-profile→converse flow works) |
| Public account on prod Godseye dashboard | TO ADD (prod AionUI is admin-only today) |
| Marketplace / plugins | "Coming soon" (scarcity) — DV curates later |
| Base vanilla recursive agent instance | Have it (the Hermes OSS instance we run) |
| Connect own Telegram bot | Build |
| OpenCode + GLM key resale + referral links | Wire both referral links into signup |
| Pricing rewrite (main + G4, off WordPress) | DO NOW |

---

## 8. COMMUNITY (Living with Agents)
- Open-source community page: documentation, tips, hacks.
- Monthly paid membership; members get benefits because DV stays ahead on cutting-edge AI-agentic tech so they don't have to.
- "Rest assured, anything AI-agentic is all covered."
- DV is pioneering in the **no-screen, agent-runs-everything** world.
