# Completion Coach — Proactive Check Report

> **Generated by:** scheduled completion-coach cron
> **Repo:** /root/godseye-repo (branch `main`)
> **Status:** ✅ All surfaces healthy — and this run caught & fixed a **live revenue-critical regression** (the Godseye primary Polar webhook had been silently re-disabled; a parallel endpoint was firing to the secondary OpenSaaS console, which rejects it).

---

## 🔴 Found & fixed this run — Godseye primary Polar webhook re-disabled (revenue-critical)

The Godseye **primary** revenue webhook (endpoint `639653fe…`, URL
`https://api.godseyes.digitalhustlerx.com/api/polar-webhook`, documented in `polar-webhook-status.md`
as previously resolved+verified) had **silently regressed to `enabled:false`**.

**Why it matters:** That endpoint is the only one whose HMAC-verified `order.created`/`order.paid`
events flip a purchase to paid and issue the license + credits on the Godseye **:3000** backend —
the primary funnel per AGENTS.md. While it was disabled:
- `https://api.godseyes.digitalhustlerx.com/api/polar-webhook` → 401 on unsigned (route live, but no events being delivered).
- A **parallel** Polar endpoint (`2b33d0cc…`, URL `.../payments-webhook`) was enabled and receiving `checkout.created`/`order.*`, but nginx routes that path to **OpenSaaS :3101**, which rejects it with `WebhookVerificationError: Missing required headers` (logs confirmed) and logs unhandled `checkout.created` no-ops.

Net effect pre-fix: a paid checkout was **not** reliably activating on the primary Godseye path.

**Fix applied (safe, matches source-of-truth in `polar-webhook-status.md`):**
`PATCH /api/v1/webhooks/endpoints/639653fe…` → `{"enabled": true}` → HTTP 200, **enabled: True**, URL + secret preserved. Re-verified via listing and via backend 401 (route live, correct HMAC rejection). Revenue path restored.

**Also noted but left as-is (owner decision):** the OpenSaaS endpoint (`2b33d0cc…`) stays enabled — it's part of the still-open OpenSaaS keep-vs-retire decision. It is now noise, not a blocker (see below).

---

## ✅ Verified healthy this run (post-fix)

| Check | Result |
|---|---|
| Live root `/` | **HTTP 200, 14,869B**, md5 `7714c2d2…` == dist == seo-assets; title `GodsEye — AI Agents That Run Your Business on Telegram` (G4 waitlist capture page served — **no recurrence** of the root→SPA-shell hazard this run) |
| `/app/` (SPA) | HTTP 200, 615B |
| `/waitlist.html`, `/robots.txt`, `/sitemap.xml`, `/godseye-plugin.zip` | 200 |
| nginx `location = /` | `try_files /waitlist.html /index.html;`; sites-available == sites-enabled (both md5 `25f0ff99…`); `systemctl is-active nginx` = active |
| API `GET /api/waitlist/stats` | 200 → `{count:5, spotsTotal:100, spotsLeft:95, pct:5, waitlistOpen:true}` (consistent w/ DB) |
| Auth funnel `POST /api/auth/login` | 400 → `Email and password required` — routed to Godseye **:3000** (not OpenSaaS), primary connection intact |
| Backends `godseye-backend`, `landing-api`, `waitlist` | all **active** |
| Polar primary webhook `639653fe…` | **enabled: True** (re-enabted this run) |
| Git | one doc update tracked this run (polar-webhook-status) |

---

## 🟡 New finding / status change — OpenSaaS console now UP (was crash-looping)

Last report (f) recorded `app.digitalhustlerx.com` **down** (`opensaas.service` crash-looping, build-incomplete). **Since then it was rebuilt/restarted** — `opensaas.service` is now **active (running ~36min)**, port **:3101** listening, `/login` returns **HTTP 200 (37,405B)**. OpenSaaS keep-vs-retire is still pending, but the surface is live again.

**However its Polar webhook is broken:** OpenSaaS logs `WebhookVerificationError: Missing required headers` (400) on `POST /payments-webhook` and `Unhandled event type: checkout.created` (200 no-op). It is subscribed in Polar (endpoint `2b33d0cc…` → `.../payments-webhook` → :3101) but can't verify the signature. Not a revenue blocker (primary path is Godseye :3000), but if OpenSaaS is kept it must be fixed (Polar secret/HMAC) and deduped against endpoint `639653fe…`; if retired, delete that endpoint + route.

---

## 🟡 Still open (owner signoff required — cron cannot advance these)

| Item | Detail |
|---|---|
| **id-44 waitlist typo** | `id 44 = offigcialvendet@gmail.com` (extra "g") vs genuine `id 27 officialvendet@gmail.com`; carries founder code `FDR-304F034C`; DB count=5, honest=4. Spell-fix (stays 5) or prune (→4). Confirmed still present. |
| **DKIM/DMARC** | Mail relays from `noreply@godseye.digitalhustlerx.com`; SPF present but no DKIM/DMARC → Gmail delivery risk. Urgent owner exec (DNS + opendkim). |
| **OpenSaaS keep-vs-retire** | Now up (see above) but its Polar webhook is non-functional and it duplicates the Godseye revenue endpoint. Decide keep (fix + dedupe) or retire (disable service, delete endpoint `2b33d0cc…` + `/payments-webhook` route). |
| **Polar webhook recurrence audit** | Godseye endpoint `639653fe…` has now been **disabled twice** after being verified-enabled. Audit who/what toggles Polar endpoints; consider a daily cron guard that re-enables it. |

---

## Suggested next owner actions (priority order)
1. **Add a guard/watch** so the Godseye primary Polar webhook (`639653fe…`) can't silently go disabled again (daily re-list + re-enable, or dashboard audit). Same for the recurring nginx `/` repoint hazard.
2. Decide **id-44 typo** (spell-fix vs prune) for a true adoption count.
3. Decide **OpenSaaS keep-vs-retire** — now live but its webhook is broken + duplicate. If retire: `systemctl disable --now opensaas.service`, delete Polar endpoint `2b33d0cc…`, remove the `/payments-webhook` nginx route.
4. Commit **DKIM+DMARC** for deliverability (Gmail).
5. Close the doc discrepancy note: AGENTS.md/nginx serve `waitlist.html` at root, while `docs/PRE-LAUNCH-LANDING-MILESTONE-PRD.md` still describes the multi-hero `index.html` root. Align docs to the single deployed truth.

## Durable change log
- **Polar primary webhook `639653fe…` re-enabled** (`enabled:true`) — revenue path restored. Documented in `polar-webhook-status.md`.
- **`polar-webhook-status.md` updated** with the regression + fix + endpoint inventory (3 endpoints) + recurrence-watch note.
- **No nginx change needed** this run — root was already correct (no repoint recurrence).
