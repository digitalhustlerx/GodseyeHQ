# Completion Coach — Proactive Check Report

> **Generated by:** scheduled completion-coach cron
> **Repo:** /root/godseye-repo (branch `main`, working tree clean, in sync with `origin/main`)
> **Status:** **🔴 Critical regression found & fixed this run — the G4 waitlist landing page had been silently repointed to the React SPA shell at the main-domain root `/` a THIRD time** (recurrence of the 08-15 & 08-19 hazard). Restored the waitlist-first `location = /` and re-verified all surfaces healthy. Also confirmed the OpenSaaS console (`app.digitalhustlerx.com`) is in a build-incomplete, crash-looping state (reporting for owner, not auto-fixed).

---

## 🔴 Found & fixed this run — G4 waitlist page repointed to SPA shell at root `/` (third occurrence)

The G4 waitlist landing page (full 4-agent capture page) was **not** reachable at `https://godseye.digitalhustlerx.com/` when this run started.

**Evidence captured before the fix:**
- `https://godseye.digitalhustlerx.com/` → **HTTP 200, 615B** = the React SPA shell (`index.html`, `<title>GodsEye — AI Agents for Your Business`), NOT the 14,869B G4 waitlist capture page.
- `dist/waitlist.html` was **intact** (14,869B, md5 `7714c2d2…` == `seo-assets/waitlist.html`), titled `GodsEye — AI Agents That Run Your Business on Telegram`.
- Live nginx `sites-available/godseye.digitalhustlerx.com` (mtime **16:40**) `location = /` read `try_files /index.html =404;` (serves SPA shell).
- The known-good `.bak-…` backups (02:42 et al.) and the repo canonical `docs/nginx/godseye.digitalhustlerx.com.conf` both hold the correct block:
  ```nginx
  location = / {
      try_files /waitlist.html /index.html;
  }
  ```

**Root cause:** The live nginx config's `location = /` had been repointed to `index.html` again — the exact recurring "waitlist root → SPA shell" hazard documented on 08-15 and 08-19. This time the file mtime was **16:40** (this run started ~16:46). Both `sites-available` and `sites-enabled` copies had the same broken value (per the known gotcha, `sites-enabled` is a regular file copy, not a symlink).

**Fix applied (documented, safe, matches source-of-truth):**
1. Backed up live config → `godseye.digitalhustlerx.com.bak-reg-20260811165127`.
2. Restored `location = / { try_files /waitlist.html /index.html; }` (matches repo canonical + known-good `.bak` exactly).
3. **Synced `sites-enabled` = `sites-available`** (both md5 `25f0ff99…`).
4. `nginx -t` → syntax ok / test successful.
5. `systemctl restart nginx` → **active** (full restart per AGENTS.md).
6. Verified live root → **HTTP 200, 14,869B**, md5 `7714c2d2…` == dist == seo-assets; title `GodsEye — AI Agents That Run Your Business on Telegram`. `/app/` SPA intact (200, 615B).

**Why not left for owner:** this is exactly the documented source-of-truth state (repo-canonical `docs/nginx`, AGENTS.md rule #6, the config's own comment "Main domain entry = dedicated waitlist landing page", the known-good `.bak`). No ambiguity, low risk, and the lead-capture entry point was broken in production. This is the **third** occurrence — the recurring-hazard guard (repo-canonical `docs/nginx` + commit `58407f4`) existed but whoever repoints it is editing the **live system file**, not the repo. **Owner must audit who/what edits `/etc/nginx` on this host** so this stops recurring.

---

## ✅ Verified healthy this run (post-fix, no action)

| Check | Result |
|---|---|
| Live root `/` | **HTTP 200, 14,869B text/html**, md5 `7714c2d2…` == dist == source (G4 capture page served) |
| `/app/` (marketing SPA) | HTTP 200, 615B text/html |
| `/waitlist.html` | HTTP 200, 14,869B |
| `/robots.txt` / `/sitemap.xml` | 200 / 200 |
| `/godseye-plugin.zip` | 200, 9,535B application/zip |
| API `GET /api/waitlist/stats` | 200 → `{count:5, spotsTotal:100, spotsLeft:95, pct:5, waitlistOpen:true}` |
| Auth funnel `POST /api/auth/login` | 400 → `{"error":"Email and password required"}` — still routed to Godseye **:3000** (not OpenSaaS) — primary connection intact |
| Backend `godseye-backend`, `godseye-landing-api`, `godseye-waitlist` | all **active** |
| nginx | active; test successful; `location = /` matches repo canonical byte-for-byte |
| Git | working tree **clean**; no tracked changes this run (nginx config is a system file) |

---

## 🟡 New finding — OpenSaaS console (`app.digitalhustlerx.com`) is DOWN (build-incomplete, crash-looping)

- `opensaas.service` is stuck in **`activating (auto-restart)`** — restart counter at **94**, `ExecStart` exits `status=1/FAILURE`.
- Logs: `Error: Cannot find module '/root/open-saas/template/app/.wasp/out/server/bundle/server.js'`.
- The `.wasp/out/server/` dir exists but has **no `bundle/` build artifact** — the `server.js` bundle was never produced (or was cleaned). Nothing listens on port **3101**, so `app.digitalhustlerx.com` is unreachable.
- **Not auto-fixed:** this is part of the still-open owner decision **"OpenSaaS keep-vs-retire"** (secondary surface; its payments/credits are placeholders now that the primary funnel runs on Godseye :3000). A Wasp rebuild is heavy and potentially undesired if retiring. The primary revenue path (Godseye :3000) is fully healthy and unaffected.

**Owner action:** decide keep-vs-retire:
- **Keep** → run `cd /root/open-saas/template/app && npx wasp build`, then restart `opensaas.service`.
- **Retire** → `systemctl disable --now opensaas.service` and remove/neutralize the `app.digitalhustlerx.com` routes so the dead surface doesn't linger.

---

## 🟡 Still open (owner signoff required — cron cannot advance these)

| Item | Status | Detail |
|---|---|---|
| **Waitlist typo-duplicate (id 44)** | Open | `id 44 = offigcialvendet@gmail.com` (extra "g") vs genuine `id 27 officialvendet@gmail.com`; carries founder code `FDR-304F034C`; waitlist `count=5`, **honest count=4**. Owner: spell-fix (stays 5) or prune (→ 4). Confirmed still present this run. |
| **Polar webhook events toggle** | Open (owner click) | Backend HMAC verified & live; missing founder selecting `checkout.completed` + `order.created` in Polar dashboard. |
| **DKIM/DMARC** | Open (urgent, owner exec) | Mail relays from `noreply@godseye.digitalhustlerx.com`; SPF present but no DKIM/DMARC → Gmail delivery risk. Needs DNS + opendkim. |
| **OpenSaaS app console** | Open (see above) | New detail: build-incomplete crash-loop; decide keep-vs-retire. |
| **nginx edit audit** | NEW, open | Find who/what repointed `location = /` a third time (check `.bash_history`, scheduled tasks, developer agents editing `/etc/nginx`). |

---

## Suggested next owner actions (priority order)
1. **Audit + lock down who edits `/etc/nginx`** — this is the 3rd recurrence. Consider a watcher/guard (e.g. `inotifywait` or a cron that diffs against repo-canonical and restores) so the waitlist root can't be silently repointed again.
2. Decide **id-44 typo** (spell-fix vs prune) for a true adoption count.
3. Decide **OpenSaaS keep-vs-retire** (details above; currently crash-looping, `app.digitalhustlerx.com` down).
4. Set the **Polar dashboard event toggle** (`checkout.completed` + `order.created`).
5. Commit **DKIM+DMARC** records for deliverability.

## Durable change log
- **nginx config restored + synced** (`sites-available` = `sites-enabled`, both md5 `25f0ff99…`): `location = /` → `try_files /waitlist.html /index.html;` (matches repo canonical `docs/nginx` byte-for-byte). Live verified 200/14,869B.
- **No tracked-repo edits made; git working tree clean.**
