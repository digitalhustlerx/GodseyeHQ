# Gemini API Key — required for AI playground
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# App URL — used for OAuth callbacks and self-referential links
APP_URL="http://localhost:3000"

# GOD-15: admin auth header value for /api/drip/* ops endpoints
# (Growth/CEO flip the waitlist->paid drip live with these).
DRIP_ADMIN_KEY="change-me-set-a-real-secret"
